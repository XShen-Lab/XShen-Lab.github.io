name: on-schedule
run-name: manual citation update

on:
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  update-citations:
    uses: ./.github/workflows/update-citations.yaml
    secrets: inherit
    with:
      open-pr: true
