name: on-push
run-name: on push to main

on:
  push:
    branches:
      - main

  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-site:
    uses: ./.github/workflows/build-site.yaml
